{
  "name": "query-benchmark",
  "description": "Benchmarks for Gatsby query performance",
  "license": "MIT",
  "scripts": {
    "develop": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "run:activity-on": "rm -rf .cache && rm -rf public && NUM_PAGES=10000  gatsby build",
    "run:activity-off": "rm -rf .cache && rm -rf public && HIDE_ACTIVITY_PROGRESS=1 NUM_PAGES=10000  gatsby build"
  },
  "dependencies": {
    "faker": "^4.1.0",
    "gatsby": "^2.0.118",
    "lodash": "^4.17.14",
    "react": "^16.8.0",
    "react-dom": "^16.8.0"
  }
}
